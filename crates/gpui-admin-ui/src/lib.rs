//! Workspace shell, views, forms, the diagnostics panel, and the layout store
//! (R5, R6, R8, R9), built on `gpui-component`. Implementation lands in Epics
//! C-D.
#![cfg_attr(not(test), deny(clippy::unwrap_used, clippy::expect_used))]
