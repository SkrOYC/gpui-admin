//! Derivation proc-macros for gpui-admin, consuming the committed schema
//! snapshot (B4). Not a dependency of `gpui-admin-core`. Implementation lands in
//! Epic F.
#![deny(clippy::unwrap_used, clippy::expect_used)]
