//! Executable conformance suite for the Provider contract (C1). Every contract
//! obligation is expressed here as a check, before or alongside the code that
//! implements it. Depends only on `gpui-admin-core`'s public contracts.
//! Implementation lands in Epic B.
#![deny(missing_docs)]
#![deny(clippy::unwrap_used, clippy::expect_used)]
